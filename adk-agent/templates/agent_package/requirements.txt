# ADK v2.2.0 固定（書籍の検証バージョン）。更新時はリリースノート確認 → 評価セット再実行
google-adk[db]==2.2.0
google-genai>=2.4,<3
pydantic>=2.0.0
python-dotenv>=1.0.0

# SessionService（staging）
aiosqlite>=0.20.0        # SQLite（ローカル検証）
asyncpg>=0.29.0          # PostgreSQL
greenlet>=3.0.0          # SQLAlchemy async engine

# テスト
pytest>=8.0.0
pytest-asyncio>=0.24.0

# 任意 extras（必要なときだけ有効化）
# google-adk[gcp,otel-gcp]==2.2.0                     # Agent Engine デプロイ / Cloud Trace
# google-cloud-aiplatform[agent-engines]==1.153.1     # Memory Bank / RAG Engine / Agent Engine SDK
# opentelemetry-exporter-otlp-proto-http==1.41.1
# google-adk[mcp]==2.2.0                              # McpToolset
# google-adk[a2a]==2.2.0 ; a2a-sdk>=0.3.24,<0.4.0     # A2A
# google-adk[extensions]==2.2.0                       # Claude（anthropic）/ LiteLLM
